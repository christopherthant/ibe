(function() {
    'use strict';

    angular
        .module(window._teromacIbeConfigApp.moduleName)
        .controller('compare', compare);

    compare.$inject = [];

    function compare() {
        var vm = this;

        activate();

        function activate() {
        }
    }
})();